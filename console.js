(()=>{'use strict';
const SUPA='https://ptblnpiroqftcvlsrhac.supabase.co',KEY='sb_publishable_SqXIeGN-clcZ4gjmpLdSww_4DLfyy24';
if(!window.supabase)return;
const sb=window.supabase.createClient(SUPA,KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});
const state={overview:null,last:{idx:null,sup:null,push:null},period:'today'};
const css=`
.kcx-ledrow{display:grid!important;grid-template-columns:auto 11px;grid-template-rows:auto 11px;gap:2px 6px;align-items:center;cursor:pointer;min-width:64px}.kcx-traffic{width:9px;height:9px;border-radius:50%;background:#8a7a67;opacity:.25;transition:.08s}.kcx-traffic.flash{background:#f2bd22;opacity:1;box-shadow:0 0 9px #f2bd22}.kcx-dot{display:inline-block;width:11px;height:11px;border-radius:50%;vertical-align:-1px;margin-right:6px;background:#999;box-shadow:0 0 0 1px rgba(0,0,0,.08)}.kcx-dot.green{background:#24834f;box-shadow:0 0 7px #24834f}.kcx-dot.yellow{background:#d69715;box-shadow:0 0 7px #d69715}.kcx-dot.red{background:#bd2f2f;box-shadow:0 0 7px #bd2f2f}.kcx-dot.blue{background:#2878c7;box-shadow:0 0 7px #2878c7}.kcx-dot.gray{background:#999}.kcx-small{font-size:12px;color:#6d6966}.kcx-table{width:100%;border-collapse:collapse}.kcx-table th,.kcx-table td{padding:10px 8px;border-bottom:1px solid #e7dfd6;text-align:left;vertical-align:top}.kcx-table th{font-size:11px;text-transform:uppercase;letter-spacing:.04em;color:#746b64}.kcx-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:10px}.kcx-provider{border:1px solid #ded6cd;border-radius:13px;padding:12px;background:#fff}.kcx-provider h3{margin:0 0 7px}.kcx-route{display:grid;grid-template-columns:1fr auto;gap:5px;border-top:1px solid #eee5dc;padding:7px 0}.kcx-pill{border-radius:999px;padding:4px 8px;font-size:11px;font-weight:800;background:#eee}.kcx-pill.ok{background:#e7f4eb;color:#237447}.kcx-pill.warn{background:#fff2dc;color:#9a6309}.kcx-pill.bad{background:#fff0ef;color:#922e2e}.kcx-pill.info{background:#eaf2fb;color:#24639a}.kcx-action{border:1px solid #ded6cd;background:#fff;border-radius:9px;padding:8px 10px;font-weight:700;cursor:pointer}.kcx-action.primary{background:#741521;color:white;border-color:#741521}.kcx-note{padding:10px 12px;border-radius:10px;background:#f3eee7;margin-top:10px}.kcx-error{background:#fff0ef;color:#922e2e}.kcx-program-strip{display:flex;gap:8px;align-items:center;flex-wrap:wrap;background:#fff;border:1px solid #ded6cd;border-radius:12px;padding:9px 11px;margin-bottom:10px}.kcx-program-chip{display:inline-flex;align-items:center;gap:3px;font-size:12px}.kcx-kpis{display:grid;grid-template-columns:repeat(8,minmax(90px,1fr));gap:7px}.kcx-kpi{background:#fff;border:1px solid #ded6cd;border-radius:11px;padding:9px}.kcx-kpi span{display:block;font-size:10px;text-transform:uppercase;color:#746b64;font-weight:800}.kcx-kpi b{display:block;font-size:20px;margin-top:3px}.kcx-dashhead{display:flex;gap:8px;align-items:flex-start;justify-content:space-between;margin-bottom:9px}.kcx-periods{display:flex;gap:4px}.kcx-periods button{border:1px solid #ded6cd;background:#fff;border-radius:8px;padding:6px 8px;font-size:11px;cursor:pointer}.kcx-periods button.active{background:#741521;color:#fff}.kcx-dashgrid{display:grid;grid-template-columns:1.25fr .75fr 1fr;gap:9px}.kcx-card{background:#fff;border:1px solid #ded6cd;border-radius:12px;padding:10px;min-height:140px}.kcx-card.wide{grid-column:span 2}.kcx-card h3{margin:0 0 7px;font-size:12px;text-transform:uppercase;color:#4a3330}.kcx-bars{display:grid;gap:5px}.kcx-bar{display:grid;grid-template-columns:100px 1fr 34px;gap:6px;align-items:center;font-size:11px}.kcx-bar span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.kcx-bar b{height:8px;background:#eee7df;border-radius:999px;overflow:hidden}.kcx-bar b i{display:block;height:100%;background:#6d6966;border-radius:999px}.kcx-bar em{font-style:normal;text-align:right;font-weight:800}.kcx-line svg{width:100%;height:110px;display:block}.kcx-line .axis{stroke:#d4cbc1;stroke-width:1}.kcx-line .trend{stroke:#741521;stroke-width:2;fill:none}.kcx-line circle{fill:#741521}.kcx-axis{display:flex;justify-content:space-between;font-size:9px;color:#746b64}.kcx-donutrow{display:flex;align-items:center;gap:14px;padding-top:6px}.kcx-donut{width:90px;height:90px;border-radius:50%;background:conic-gradient(#24834f 0 var(--a),#d69715 var(--a) var(--b),#bd2f2f var(--b) 360deg);position:relative}.kcx-donut:after{content:'';position:absolute;inset:18px;background:white;border-radius:50%}.kcx-legend{display:grid;gap:6px;font-size:11px}.kcx-health{display:grid;gap:9px;font-size:12px}.kcx-modal{border:1px solid #ded6cd;border-radius:18px;width:min(760px,95vw);padding:20px;box-shadow:0 18px 55px rgba(30,20,15,.25)}.kcx-modal::backdrop{background:rgba(20,15,12,.45)}.kcx-gauges{display:grid;grid-template-columns:1fr 1fr;gap:18px;margin:16px 0}.kcx-gauge{display:grid;place-items:center}.kcx-dial{--p:0;display:grid;place-items:center;width:145px;height:145px;border-radius:50%;background:conic-gradient(#24834f calc(var(--p)*1%),#ece5de 0);position:relative}.kcx-dial:before{content:'';position:absolute;inset:13px;border-radius:50%;background:white}.kcx-dial b,.kcx-dial small{position:relative;z-index:1}.kcx-dial b{font-size:25px}.kcx-dial small{display:block;text-align:center;color:#6d6966}@media(max-width:1200px){.kcx-kpis{grid-template-columns:repeat(4,minmax(90px,1fr))}.kcx-dashgrid{grid-template-columns:1fr 1fr}.kcx-card.wide{grid-column:span 2}}@media(max-width:760px){.kcx-kpis{grid-template-columns:repeat(2,minmax(100px,1fr))}.kcx-dashgrid{grid-template-columns:1fr}.kcx-card.wide{grid-column:auto}.kcx-gauges{grid-template-columns:1fr}}
`;
const st=document.createElement('style');st.textContent=css;document.head.appendChild(st);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
const rowTime=x=>new Date(x?.created_at||x?.sent_at||x?.processed_at||0).getTime()||0;
const status=x=>String(x?.status||'').toLowerCase();
const ok=x=>['sent','delivered','success','ok','accepted','processed'].includes(status(x));
const fail=x=>['failed','dead_lettered','error','rejected'].includes(status(x));
const retry=x=>['retry_scheduled','retry','partial','queued'].includes(status(x));
function flash(kind){const e=document.getElementById('kcx-'+kind+'-traffic');if(!e)return;e.classList.add('flash');clearTimeout(e._t);e._t=setTimeout(()=>e.classList.remove('flash'),220)}
const origFetch=window.fetch.bind(window);window.fetch=async(...a)=>{const u=String(a[0]?.url||a[0]||'');if(u.includes('supabase.co'))flash(u.includes('push')?'push':'sup');return origFetch(...a)};
try{const o=indexedDB.open.bind(indexedDB);indexedDB.open=(...a)=>{flash('idx');return o(...a)}}catch{}
async function api(action){flash('sup');const t=performance.now();const {data,error}=await sb.functions.invoke('kc-communication-console',{body:{action}});if(error)throw error;return {data,total:performance.now()-t}}
async function providerHealth(){flash('push');const t=performance.now();const {data,error}=await sb.functions.invoke('kc-communication-provider-health',{body:{}});if(error)throw error;return {data,total:performance.now()-t}}
async function getOverview(force=false){if(state.overview&&!force)return state.overview;const r=await api('overview');if(!r.data?.ok)throw new Error(r.data?.error||'Übersicht nicht verfügbar');state.overview=r.data;return state.overview}
function enhanceLEDs(){document.querySelectorAll('.dbs .db').forEach((box,i)=>{if(box.dataset.kcx)return;box.dataset.kcx='1';const k=['idx','sup','push'][i]||'sup';box.classList.add('kcx-ledrow');box.insertAdjacentHTML('beforeend',`<span class="kcx-small">Traffic</span><i id="kcx-${k}-traffic" class="kcx-traffic"></i>`);box.title='Info, Messwerte und Verbindungstest';box.addEventListener('click',()=>openDiag(k))})}
function progColor(p,c,reqs=[]){if(p?.status==='disabled'||p?.status==='error')return ['red','Fehler'];const last=reqs.filter(r=>String(r.source_program||'')===String(p?.id)).sort((a,b)=>rowTime(b)-rowTime(a))[0];if(last&&fail(last)&&Date.now()-rowTime(last)<3600000)return ['red','letzter Vorgang fehlerhaft'];if(c?.status==='active'){const age=c.last_seen_at?Date.now()-new Date(c.last_seen_at).getTime():Infinity;if(age<10*60000)return ['green','live verbunden'];if(age<30*60000)return ['yellow','Kontakt verzögert'];return ['blue','freigegeben, derzeit ruhig']}if(c?.status==='pending')return ['blue','wartet auf Freigabe'];if(p?.status==='active')return ['blue','aktiv / kein Live-Client'];return ['gray','geplant']}
function periodRows(rows){const h={today:24,d7:168,d30:720,all:null}[state.period];const s=[...(rows||[])].sort((a,b)=>rowTime(b)-rowTime(a));if(!h)return s;const min=Date.now()-h*3600000;return s.filter(x=>rowTime(x)>=min)}
function group(rows,fn){const m=new Map;for(const r of rows){const k=String(fn(r)||'unbekannt');m.set(k,(m.get(k)||0)+1)}return [...m.entries()].sort((a,b)=>b[1]-a[1])}
function bars(a){const m=Math.max(1,...a.map(x=>x[1]));return `<div class="kcx-bars">${a.map(([n,v])=>`<div class="kcx-bar"><span title="${esc(n)}">${esc(n)}</span><b><i style="width:${Math.max(2,v/m*100)}%"></i></b><em>${v}</em></div>`).join('')||'<div class="kcx-small">Noch keine Daten.</div>'}</div>`}
function buckets(rows){const src=periodRows(rows),end=Date.now();let start=src.length?Math.min(...src.map(rowTime)):end-24*3600000;const h={today:24,d7:168,d30:720,all:null}[state.period];if(h)start=end-h*3600000;if(start===end)start=end-3600000;const arr=[],step=(end-start)/12;for(let i=0;i<12;i++){const a=start+i*step,b=i===11?end+1:start+(i+1)*step,d=new Date(a);arr.push({label:h&&h<=24?String(d.getHours()).padStart(2,'0')+'h':d.toLocaleDateString('de-DE',{day:'2-digit',month:'2-digit'}),count:src.filter(x=>rowTime(x)>=a&&rowTime(x)<b).length})}return arr}
function line(points){const w=520,h=120,p=10,m=Math.max(1,...points.map(x=>x.count)),step=(w-p*2)/Math.max(1,points.length-1),pts=points.map((x,i)=>`${p+i*step},${h-p-(x.count/m)*(h-p*2)}`).join(' ');return `<div class="kcx-line"><svg viewBox="0 0 ${w} ${h}"><line x1="10" y1="110" x2="510" y2="110" class="axis"/><polyline points="${pts}" class="trend"/>${points.map((x,i)=>`<circle cx="${p+i*step}" cy="${h-p-(x.count/m)*(h-p*2)}" r="3"><title>${esc(x.label)}: ${x.count}</title></circle>`).join('')}</svg><div class="kcx-axis"><span>${esc(points[0]?.label||'')}</span><span>${esc(points.at(-1)?.label||'jetzt')}</span></div></div>`}
async function renderDashboard(){const el=document.getElementById('content');if(!el)return;el.innerHTML='<section class="card"><h2>Dashboard</h2><div class="kcx-note">Live-Daten werden geladen …</div></section>';try{const o=await getOverview(true),all=[...(o.requests||[])].sort((a,b)=>rowTime(b)-rowTime(a)),rows=periodRows(all),oks=rows.filter(ok).length,fails=rows.filter(fail).length,retries=rows.filter(retry).length,total=rows.length,programs=o.programs||[],clients=o.clients||[],providers=o.providers||[],routes=o.routes||[];const strip=programs.map(p=>{const c=clients.find(x=>x.source_program===p.id),s=progColor(p,c,all);return `<span class="kcx-program-chip" title="${esc(s[1])}"><i class="kcx-dot ${s[0]}"></i>${esc(p.display_name||p.id)}</span>`}).join('');const success=total?Math.round(oks*1000/total)/10:0,a=total?oks/total*360:0,b=total?(oks+retries)/total*360:a;const lastOk=all.find(ok);const pStates=routes.slice(0,8).map(r=>{const p=providers.find(x=>x.id===r.provider_id);return `<div><i class="kcx-dot ${r.enabled?(r.health_status==='bad'?'red':'green'):'gray'}"></i><b>${esc(String(r.channel||'').toUpperCase())}</b> · ${esc(p?.display_name||r.provider_id)} <span class="kcx-small">${esc(r.role||'')} / Prio ${esc(r.priority||'')}</span></div>`}).join('')||'<div class="kcx-small">Keine Providerdaten.</div>';el.innerHTML=`<section><div class="kcx-program-strip"><b>Programme</b>${strip||'<span class="kcx-small">Keine Programme hinterlegt.</span>'}</div><div class="kcx-dashhead"><div class="kcx-kpis"><div class="kcx-kpi"><span>Vorgänge</span><b>${total}</b></div><div class="kcx-kpi"><span>OK</span><b>${oks}</b></div><div class="kcx-kpi"><span>Teil/Retry</span><b>${retries}</b></div><div class="kcx-kpi"><span>Fehler</span><b>${fails}</b></div><div class="kcx-kpi"><span>Erfolgsquote</span><b>${success}%</b></div><div class="kcx-kpi"><span>Programme</span><b>${programs.length}</b></div><div class="kcx-kpi"><span>Provider</span><b>${providers.length}</b></div><div class="kcx-kpi"><span>Letzter OK</span><b style="font-size:15px">${lastOk?new Date(rowTime(lastOk)).toLocaleTimeString('de-DE',{hour:'2-digit',minute:'2-digit'}):'–'}</b></div></div><div class="kcx-periods">${[['today','Heute'],['d7','7 Tage'],['d30','30 Tage'],['all','Gesamt']].map(([k,l])=>`<button data-kcx-period="${k}" class="${state.period===k?'active':''}">${l}</button>`).join('')}</div></div><div class="kcx-dashgrid"><article class="kcx-card wide"><h3>Versandaufkommen</h3>${line(buckets(all))}</article><article class="kcx-card"><h3>Ergebnis</h3><div class="kcx-donutrow"><div class="kcx-donut" style="--a:${a}deg;--b:${b}deg"></div><div class="kcx-legend"><span><i class="kcx-dot green"></i>OK <b>${oks}</b></span><span><i class="kcx-dot yellow"></i>Teil/Retry <b>${retries}</b></span><span><i class="kcx-dot red"></i>Fehler <b>${fails}</b></span></div></div></article><article class="kcx-card"><h3>Nach Kanal</h3>${bars(group(rows,x=>x.channel).slice(0,7))}</article><article class="kcx-card"><h3>Nach Programm</h3>${bars(group(rows,x=>x.source_program).slice(0,7))}</article><article class="kcx-card wide"><h3>Provider & Failover</h3><div class="kcx-health">${pStates}</div></article><article class="kcx-card"><h3>Systemzustand</h3><div class="kcx-health"><span><i class="kcx-dot green"></i>Supabase angemeldet</span><span><i class="kcx-dot ${programs.some(p=>String(p.id).includes('backup')||String(p.display_name||'').toLowerCase().includes('backup'))?'green':'blue'}"></i>PC Backup Vault ${programs.some(p=>String(p.id).includes('backup')||String(p.display_name||'').toLowerCase().includes('backup'))?'registriert':'noch nicht erkannt'}</span><span><i class="kcx-dot ${routes.some(r=>r.channel==='push'&&r.enabled)?'green':'yellow'}"></i>Push ${routes.some(r=>r.channel==='push'&&r.enabled)?'aktiv':'prüfen'}</span><span><i class="kcx-dot ${routes.some(r=>r.channel==='email'&&r.enabled)?'green':'yellow'}"></i>E-Mail ${routes.some(r=>r.channel==='email'&&r.enabled)?'aktiv':'prüfen'}</span></div></article></div></section>`;el.querySelectorAll('[data-kcx-period]').forEach(b=>b.onclick=()=>{state.period=b.dataset.kcxPeriod;renderDashboard()})}catch(e){el.innerHTML=`<section class="card"><h2>Dashboard</h2><div class="kcx-note kcx-error">${esc(e.message||e)}</div></section>`}}
async function renderPrograms(){const el=document.getElementById('content');if(!el)return;el.innerHTML='<section class="card"><h2>Programme & Verbindungen</h2><div class="kcx-note">Live-Daten werden geladen …</div></section>';try{const o=await getOverview(true),req=o.requests||[];el.innerHTML=`<section class="card"><div style="display:flex;justify-content:space-between;gap:10px;align-items:center"><div><h2 style="margin-bottom:4px">Programme & Verbindungen</h2><div class="muted">Grün = live · Gelb = verzögert · Rot = Fehler · Blau = freigegeben/ruhig · Grau = geplant.</div></div><button class="kcx-action" id="kcxProgReload">Aktualisieren</button></div><div style="overflow:auto;margin-top:10px"><table class="kcx-table"><thead><tr><th>Status</th><th>Programm</th><th>Client / Gerät</th><th>Letzter Kontakt</th><th>Versandwege</th></tr></thead><tbody>${(o.programs||[]).map(p=>{const c=(o.clients||[]).find(x=>x.source_program===p.id),s=progColor(p,c,req);return `<tr><td><i class="kcx-dot ${s[0]}"></i><b>${esc(s[1])}</b></td><td><b>${esc(p.display_name||p.id)}</b><div class="kcx-small">${esc(p.id)}</div></td><td>${c?esc(c.device_name||'Maschinen-Client'):'–'}${c?`<div class="kcx-small">Status: ${esc(c.status)}</div>`:''}</td><td>${c?.last_seen_at?new Date(c.last_seen_at).toLocaleString('de-DE'):'noch kein Heartbeat'}</td><td>${(p.allowed_channels||[]).map(x=>`<span class="kcx-pill info">${esc(String(x).toUpperCase())}</span>`).join(' ')||'–'}</td></tr>`}).join('')}</tbody></table></div></section>`;document.getElementById('kcxProgReload').onclick=renderPrograms}catch(e){el.innerHTML=`<section class="card"><h2>Programme & Verbindungen</h2><div class="kcx-note kcx-error">${esc(e.message||e)}</div></section>`}}
async function renderProviders(){const el=document.getElementById('content');if(!el)return;el.innerHTML='<section class="card"><h2>Provider & Versandwege</h2><div class="kcx-note">Provider werden geladen …</div></section>';try{const o=await getOverview(true),by={};(o.routes||[]).forEach(r=>(by[r.channel]??=[]).push(r));const channels=['email','push','sms','whatsapp'];el.innerHTML=`<section class="card"><h2>Provider & Versandwege</h2><p class="muted">Mehrere Provider pro Kanal, Priorität, Auto-Failover und Kostensperre.</p><div class="kcx-grid">${channels.map(ch=>`<div class="kcx-provider"><h3>${ch==='email'?'✉️ E-Mail':ch==='push'?'🔔 Push':ch==='sms'?'💬 SMS':'🟢 WhatsApp'}</h3>${(by[ch]||[]).map(r=>{const p=(o.providers||[]).find(x=>x.id===r.provider_id),c=!r.enabled?'gray':r.health_status==='bad'?'red':'green',lbl=r.cost_policy==='locked_until_approved'?'Kostensperre':r.enabled?'aktiv':'inaktiv';return `<div class="kcx-route"><div><i class="kcx-dot ${c}"></i><b>${esc(p?.display_name||r.provider_id)}</b><div class="kcx-small">${esc(r.role)} · Priorität ${esc(r.priority)} · ${r.auto_failover?'Auto-Failover':'kein Auto-Failover'}</div><div class="kcx-small">Kostenregel: ${esc(r.cost_policy||'')}</div></div><span class="kcx-pill ${c==='green'?'ok':c==='red'?'bad':'info'}">${esc(lbl)}</span></div>`}).join('')||'<div class="kcx-small">Noch kein Provider hinterlegt.</div>'}</div>`).join('')}</div><div class="actions"><button class="kcx-action primary" id="kcxProviderTest">Provider prüfen</button></div><div id="kcxProviderOut" class="kcx-note">Noch kein Test gestartet.</div></section>`;document.getElementById('kcxProviderTest').onclick=async()=>{const out=document.getElementById('kcxProviderOut');out.textContent='Providerprüfung läuft …';try{const r=await providerHealth(),a=r.data?.providers||[];out.innerHTML=a.map(x=>`<div><i class="kcx-dot ${x.outboundReady?'green':x.costLocked?'blue':'yellow'}"></i><b>${esc(String(x.channel||'').toUpperCase())}</b> · ${x.provider?esc(x.provider):'kein Provider'} · ${x.outboundReady?'versandbereit':x.costLocked?'Kostensperre':'noch nicht produktionsbereit'}</div>`).join('')||'Keine Providerdaten.'}catch(e){out.className='kcx-note kcx-error';out.textContent='Provider-Test fehlgeschlagen: '+(e.message||e)}}}catch(e){el.innerHTML=`<section class="card"><h2>Provider & Versandwege</h2><div class="kcx-note kcx-error">${esc(e.message||e)}</div></section>`}}
function ensureModal(){let d=document.getElementById('kcxDiag');if(d)return d;d=document.createElement('dialog');d.id='kcxDiag';d.className='kcx-modal';d.innerHTML='<div id="kcxDiagBody"></div><div style="display:flex;justify-content:flex-end;margin-top:12px"><button class="kcx-action" id="kcxClose">Schließen</button></div>';document.body.appendChild(d);d.querySelector('#kcxClose').onclick=()=>d.close();return d}
function gauge(label,val,max=1000){const n=Math.max(0,Number(val)||0),p=Math.max(2,Math.min(100,n/max*100));return `<div class="kcx-gauge"><div class="kcx-dial" style="--p:${p}"><div style="text-align:center"><b>${Math.round(n)} ms</b><small>${esc(label)}</small></div></div></div>`}
async function localHealth(){const t=performance.now();await new Promise((ok,bad)=>{const r=indexedDB.open('kc-communication-public',3);r.onupgradeneeded=()=>{if(!r.result.objectStoreNames.contains('health'))r.result.createObjectStore('health')};r.onsuccess=()=>{r.result.close();ok()};r.onerror=()=>bad(r.error)});return performance.now()-t}
async function openDiag(kind){const d=ensureModal(),b=d.querySelector('#kcxDiagBody');b.innerHTML='<h2>Verbindungsdiagnose</h2><div class="kcx-note">Messung läuft …</div>';d.showModal();try{let a=0,z=0,txt='';if(kind==='idx'){a=z=await localHealth();txt='Lokaler IndexedDB-Zugriff.'}else if(kind==='sup'){const r=await api('health');a=Number(r.data?.latencyMs||r.data?.dbMs||0);z=r.total;txt='Supabase / KC Communication antwortet.'}else{const r=await providerHealth();a=Math.max(1,r.total*.35);z=r.total;txt='Push / Provider-Health wurde geprüft.'}b.innerHTML=`<h2>${kind==='idx'?'IndexedDB':kind==='sup'?'Supabase':'Push'} Diagnose</h2><div class="kcx-gauges">${gauge('Reaktion',a)}${gauge('Gesamtlaufzeit',z)}</div><div class="kcx-note">${esc(txt)}</div>`}catch(e){b.innerHTML=`<h2>Diagnose</h2><div class="kcx-note kcx-error">Test fehlgeschlagen: ${esc(e.message||e)}</div>`}}
function hookTabs(){document.querySelectorAll('.tab').forEach(btn=>{if(btn.dataset.kcxHook)return;btn.dataset.kcxHook='1';btn.addEventListener('click',()=>setTimeout(()=>{if(btn.dataset.tab==='dashboard')renderDashboard();if(btn.dataset.tab==='programs')renderPrograms();if(btn.dataset.tab==='settings')renderProviders()},0))})}
function observe(){enhanceLEDs();hookTabs();const mo=new MutationObserver(()=>{enhanceLEDs();hookTabs()});mo.observe(document.body,{childList:true,subtree:true});const active=document.querySelector('.tab.active');if(active?.dataset.tab==='dashboard'&&!document.getElementById('app')?.classList.contains('hidden'))setTimeout(renderDashboard,250)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',observe);else observe();
})();